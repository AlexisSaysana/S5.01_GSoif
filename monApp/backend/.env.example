# ========================================
# 🔒 EXEMPLE DE CONFIGURATION
# ========================================
# Copier ce fichier en .env et remplir avec vos vraies valeurs

# === DATABASE ===
DB_HOST=localhost
DB_USER=votre_utilisateur_mysql
DB_PASSWORD=votre_mot_de_passe_mysql
DB_NAME=gsoif

# === API KEYS ===
WEATHER_API_KEY=votre_cle_api_openweather

# === SERVER ===
PORT=8080

# === JWT SECRET ===
# Générer une clé sécurisée avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=generer_une_cle_secrete_longue_et_aleatoire

# === FRONTEND URL (pour CORS) ===
FRONTEND_URL=http://localhost:8081
